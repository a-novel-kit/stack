interface:
  display_name: "Plan Client/Server Boundaries"
  short_description: "Design secure primitives and composable clients"
  default_prompt: "Use $plan-client-server-boundary to decide which responsibilities belong on the server and which belong in the client."
