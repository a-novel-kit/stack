.idea

app/
kit/
tmp/

.claude/*.local.*

# Scratch planning files used by the planning workflow (see the plan-feature
# skill) — never committed. PLAN.md is the single-plan shorthand; feature work
# uses plan-<feature>[-<step>].md so several plans can coexist.
PLAN.md
plan-*.md

node_modules
/.secrets/
