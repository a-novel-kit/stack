# Backend service (Go clean-architecture). Used by service-template and
# the live services it spawns.
class: service
features:
  issues: true
  wiki: false
  projects: true
  discussions: true
merge:
  squash: true
  merge_commit: false
  rebase: false
  auto_merge: true
  delete_branch_on_merge: true
  allow_update_branch: true
  signoff_required: true
security:
  secret_scanning: true
  push_protection: true
  # Dependabot security updates are redundant with the node-audit workflow and
  # fail on these repos' private @a-novel-kit deps (no registry token for
  # Dependabot). This toggles dependabot_security_updates only; Dependabot
  # alerts (advisory detection) are a separate setting, unaffected here.
  dependabot: false
codeql:
  enabled: true
  query_suite: security-and-quality
pages: true
code_quality: true
rulesets:
  master: true
  require_approval: true
  tags: true
